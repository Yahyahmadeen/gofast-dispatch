import {useEffect,useState} from 'react';
import {getRiderOrders,updateOrderStatus,acceptOrder} from '../../services/orderService';
export default function RiderDeliveries(){const [orders,setOrders]=useState([]);const load=async()=>{const r=await getRiderOrders();if(r.success)setOrders(r.data.orders||[])};useEffect(()=>{load()},[]);const accept=async(o)=>{const r=await acceptOrder(o.id);alert(r.message);if(r.success)load()};const change=async(o,status)=>{let proof='none';if(status==='delivered'){proof=prompt('Proof type: photo, otp, or signature','otp')||'none'}const r=await updateOrderStatus(o.id,status,proof);alert(r.message);if(r.success)load()};return <section><div className="breadcrumb">Rider / <strong>My deliveries</strong></div><div className="page-title-row"><div><span className="eyebrow">RIDER OPERATIONS</span><h1>My deliveries</h1><p>Accept and progress assigned jobs through the delivery lifecycle.</p></div><button className="ghost-btn" onClick={load}>↻ Refresh</button></div><div className="panel"><div className="table-wrap"><table><thead><tr><th>Tracking</th><th>Customer</th><th>Route</th><th>Status</th><th>Action</th></tr></thead><tbody>{orders.map(o=><tr key={o.id}><td><strong>{o.tracking_number}</strong></td><td>{o.customer_name}</td><td>{o.pickup_address}<br/>→ {o.dropoff_address}</td><td><span className="status orange">{o.status.replaceAll('_',' ')}</span></td><td>{o.status==='pending'&&<button className="text-btn" onClick={()=>accept(o)}>Accept order →</button>}{o.status==='assigned'&&<button className="text-btn" onClick={()=>change(o,'picked_up')}>Picked up</button>}{o.status==='picked_up'&&<button className="text-btn" onClick={()=>change(o,'in_transit')}>In transit</button>}{o.status==='in_transit'&&<button className="text-btn" onClick={()=>change(o,'delivered')}>Deliver + proof</button>}</td></tr>)}</tbody></table></div></div></section>}
