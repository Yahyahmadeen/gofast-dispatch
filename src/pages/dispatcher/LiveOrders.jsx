import {useEffect,useState} from 'react';
import {getDispatcherOrders,assignOrder} from '../../services/orderService';
export default function LiveOrders(){const [orders,setOrders]=useState([]);const [busy,setBusy]=useState(false);const load=async()=>{const r=await getDispatcherOrders();if(r.success)setOrders(r.data.orders||[])};useEffect(()=>{load()},[]);const assign=async o=>{const rider=prompt('Enter rider user ID to assign (use a rider account ID):');if(!rider)return;setBusy(true);try{const r=await assignOrder(o.id,Number(rider));alert(r.message);if(r.success)load()}finally{setBusy(false)}};return <section><div className="breadcrumb">Dispatcher / <strong>Live orders</strong></div><div className="page-title-row"><div><span className="eyebrow">OPERATIONS CONTROL</span><h1>Live orders</h1><p>Incoming requests and active deliveries across GOFAST.</p></div><button className="ghost-btn" onClick={load}>↻ Refresh</button></div><div className="panel"><div className="table-wrap"><table><thead><tr><th>Tracking</th><th>Customer</th><th>Route</th><th>Status</th><th>Rider</th><th>Action</th></tr></thead><tbody>{orders.map(o=><tr key={o.id}><td><strong>{o.tracking_number}</strong></td><td>{o.customer_name}<br/><small>{o.customer_phone}</small></td><td>{o.pickup_address}<br/>→ {o.dropoff_address}</td><td><span className={`status ${o.status==='delivered'?'green':o.status==='pending'?'blue':'orange'}`}>{o.status.replaceAll('_',' ')}</span></td><td>{o.rider_name||'Unassigned'}</td><td>{o.status==='pending'?<button className="text-btn" disabled={busy} onClick={()=>assign(o)}>Assign rider →</button>:<span>Monitor</span>}</td></tr>)}</tbody></table></div></div></section>}
