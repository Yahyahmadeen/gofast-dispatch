import { useAuth } from '../../context/AuthContext';
export default function AdminSettings(){const {user}=useAuth();return <section><div className="breadcrumb">Administration / <strong>Settings</strong></div><div className="page-title-row"><div><span className="eyebrow">SYSTEM CONTROL</span><h1>Settings</h1><p>Keep your administrator account and platform controls organized.</p></div></div><div className="settings-grid"><div className="panel"><div className="panel-head"><div><span className="eyebrow">ACCOUNT</span><h2>Administrator profile</h2></div></div><div className="settings-form"><label>Full name<input defaultValue={user?.full_name||''}/></label><label>Email<input defaultValue={user?.email||''}/></label><label>Phone<input defaultValue={user?.phone||''}/><small>Profile editing can be connected to the admin API next.</small></label><button className="primary-btn" onClick={()=>alert('Settings form is ready. Connect the save endpoint when you want to persist edits.')}>Save changes</button></div></div><div className="panel"><div className="panel-head"><div><span className="eyebrow">PLATFORM</span><h2>GOFAST controls</h2></div></div><div className="toggle-row"><span><strong>Require rider verification</strong><small>Only approved riders can receive dispatch jobs.</small></span><input type="checkbox" defaultChecked/></div><div className="toggle-row"><span><strong>Delivery notifications</strong><small>Keep customers updated about status changes.</small></span><input type="checkbox" defaultChecked/></div><div className="toggle-row"><span><strong>Admin activity logging</strong><small>Keep a visible audit trail of critical actions.</small></span><input type="checkbox" defaultChecked/></div></div></div></section>}
