import { useState } from "react";
import { Link, useNavigate } from "react-router-dom";
import { registerCustomer } from "../../services/authService";
export default function Register(){const nav=useNavigate();const [form,setForm]=useState({full_name:"",email:"",phone:"",password:""});const [msg,setMsg]=useState("");const [loading,setLoading]=useState(false);const submit=async e=>{e.preventDefault();setLoading(true);setMsg("");try{const r=await registerCustomer(form);if(r.success){setMsg("Account created. You can now sign in.");setTimeout(()=>nav("/login"),800)}else setMsg(r.message)}catch(e){setMsg("Unable to connect to the GOFAST server.")}finally{setLoading(false)}};return <div className="auth-page register-page"><div className="auth-brand"><span className="brand-logo">G</span><span><strong>GOFAST</strong><small>Dispatch & Logistics</small></span></div><div className="register-wrap"><div className="register-card"><div className="auth-card-head"><span className="eyebrow">GET STARTED</span><h2>Create your GOFAST account</h2><p>Book deliveries, track riders and keep your history organized.</p></div>{msg&&<div className="alert">{msg}</div>}<form onSubmit={submit}><div className="form-two"><label>Full name<input value={form.full_name} onChange={e=>setForm({...form,full_name:e.target.value})} placeholder="Your full name" required/></label><label>Phone number<input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})} placeholder="080..." required/></label></div><label>Email address<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="you@company.com" required/></label><label>Password<input type="password" minLength="8" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="At least 8 characters" required/></label><button className="primary-btn wide" disabled={loading}>{loading?"Creating account...":"Create account →"}</button></form><div className="auth-footer">Already have an account? <Link to="/login">Sign in</Link></div></div><div className="register-side"><span className="eyebrow">BUILT FOR REAL OPERATIONS</span><h2>From pickup to proof of delivery.</h2><p>GOFAST gives customers, riders and operations teams one shared view of every delivery.</p><div className="feature-line"><b>✓</b><span><strong>Live tracking</strong><small>Know where every order stands.</small></span></div><div className="feature-line"><b>✓</b><span><strong>Delivery proof</strong><small>Photo + OTP or signature.</small></span></div><div className="feature-line"><b>✓</b><span><strong>Branch-ready</strong><small>Start in Yola, expand across the region.</small></span></div></div></div></div>}
